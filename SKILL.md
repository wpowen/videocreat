---
name: codex-video-workflow
description: Use when a user asks Codex to research, plan, render, QC, or package a rights-safe local video workflow, including layered SVG/HTML motion, semantic path drawing, personal-IP pages, html-video, HyperFrames, FFmpeg, voice, subtitles, and evidence logs.
---

# Codex Video Workflow

## Overview

Build local, rights-safe video packages from a brief: research notes, script, storyboard, type-specific visual design, image prompts/assets, voice direction, narration, subtitles, timecode alignment, render, cover/thumbnail design, QC, screenshots, and delivery evidence. The preferred renderer is `html-video` after the storyboard/design step; narration must use local CosyVoice or MeloTTS by default; existing `video-maker`/HyperFrames/FFmpeg remains the fallback and evidence/QC backbone.

Strict execution rule: do not replace this skill with an ad hoc static renderer, one-off SVG slideshow, PPT-style card deck, silent/synthetic placeholder, or shortcut that skips the required design, voice, animation, and QC artifacts. If a fallback is necessary, it must preserve the same design contract, motion language, local TTS policy, subtitles, evidence logs, and final QC gates, and the manifest must mark the renderer as degraded. Static/FFmpeg fallback is not a final delivery path by default; it must fail or be explicitly labeled with `--allow-degraded-renderer`.

## Use This Skill When

- The user wants a reproducible local video from a brief, article, lesson, or script.
- The user asks for layered animation, element-by-element reveal, animated lines/connectors, SVG/HTML interaction, or a less PPT-like presentation.
- The user asks whether a video repo, SDK, or renderer should be merged into the current `videocreat` workflow.
- The user wants to repair one scene, visual element, subtitle/timing segment, or another bounded node of an existing generated package without paying for a full rerun.
- The output needs proof: commands, configs, logs, screenshots, final MP4, rights record, QC scorecard, and follow-up risks.
- The topic is creator/marketing/social content, but the exact examples must be original rather than copied from YouTube, X, TikTok, or other creators.

## Do Not Use By Default

- Paid/cloud video APIs, production platform accounts, cloud GPU, private uploads, external TTS, real-person voice cloning, celebrity/likeness imitation, commercial stock, unlicensed music, unlicensed fonts, or platform-restricted assets.
- macOS `say` as a normal final-quality narration path in any language. It is allowed only when the caller explicitly passes `--allow-say-fallback`, and the manifest must mark it as degraded fallback evidence.
- OpenAI Sora Videos API as a default path: official OpenAI docs state Sora video generation models/API are deprecated and scheduled to shut down on September 24, 2026.
- Any copied creator script, shot order, packaging, persona, brand treatment, or recognizable expression. Extract only abstract methods and quality criteria.

## Workflow

1. Audit the local workspace first: read current `video-maker` docs, schemas, workflow scripts, renderer options, quality gates, and prior POC artifacts.
   - Load `assets/runtime-defaults.json` before choosing defaults. CLI flags and brief fields may override it, but ambient credentials such as `OPENAI_API_KEY` must not silently change the default route. Every run must write `workflow/runtime-config.json` with the resolved image, voice, material, cover, and frame-limit policies.
2. Research current external references only when the answer depends on current vendor/platform behavior. Prefer official docs and primary repos.
3. Create a production contract: target platform/aspect/duration, rights boundaries, script budget, visual style, voice/subtitle policy, cover/thumbnail targets, sync strategy, and QC criteria.
   - Select the product-level generation mode before planning. Default topic/script intake is `semi-auto` / configurable flow: prepare the package, generate `semi-auto-config.html`, expose page-level review, and stop before final composition unless compose was explicitly requested. Use `full-auto` only when the user explicitly asks for full-auto/全自动/一键生成, then preserve the existing one-pass plan/render/QC/package workflow.
   - Every run must write `workflow/generation-mode-contract.json` so mode choice, default horizontal behavior, supported modes, semi-auto stages, configuration sections, page-level TDS editing, capability inventory, and decision ownership are auditable.
   - Default final videos are horizontal `local-review` (`16:9`, `1920x1080`, `30fps`) unless the brief explicitly asks for a vertical canvas, portrait/`9:16`, Douyin/TikTok/Shorts/Reels, or a short-form/短视频 package. Ordinary hooks, opening beats, first-three-second promises, or口播 wording alone must not change the canvas to vertical; if a human brief is genuinely ambiguous, ask for the intended aspect before rendering.
   - For Douyin, TikTok, YouTube Shorts, Reels-like, or generic short-video requests, set the target platform to a vertical short-form profile instead of treating the result as a cropped horizontal export.
   - Vertical short-form defaults to `9:16`, `1080x1920`, `30fps`, short mobile captions, platform UI safe areas, a reserved top mobile safe band of at least `220px` for status/navigation bars, a first-frame promise, a 3-second hook, 2-4 second visual-state changes, and a payoff loop.
   - Horizontal `local-review` remains `16:9`, `1920x1080`, `30fps`.
4. Plan the video as a video, not slides:
   - Infer video type from the brief, topic, audience, platform, and scenes.
   - Choose a core template kit, visual language, motion language, shot templates, aesthetic direction, and caption-safe layout.
   - Create `workflow/generation-mode-contract.json`, `workflow/content-presentation-design.json`, `workflow/page-decision-contract.json`, `workflow/retention-structure-contract.json`, `workflow/typography-motion-plan.json`, `workflow/caption-style-plan.json`, `workflow/theme-readability-audit.json`, `workflow/quality-consistency-contract.json`, `workflow/aesthetic-brief.json`, `workflow/aesthetic-quality-rubric.md`, `workflow/motion-template-selection.json`, `workflow/motion-style-template-selection.json`, `workflow/design-plan.json`, `workflow/image-generation-strategy.json`, `workflow/image2-prompts.json`, `workflow/visual-asset-manifest.json`, `workflow/visual-relevance-audit.json`, `workflow/media-routing-plan.json`, and `workflow/visual-rhythm-plan.json` before rendering.
   - `workflow/page-decision-contract.json` is mandatory for every run. It must answer five questions for every page/scene: what content appears, how that content is designed, how interaction is represented in the rendered MP4, how animation is executed and timed, and which planning/design/render/QC layer owns each decision. In semi-auto, the page review surface must additionally support page-scoped TDS edits: Text/content, Design/layout, and Subtitle/style.
   - `workflow/retention-structure-contract.json` is mandatory for every run. Read `references/retention-structure.md` and bind the RETAIN model across the full production chain: first-frame relevance, first-30-second expectation, scene-level thinking aid, attention rhythm, information proof cadence, progress cues, ending payoff, production-phase ownership, and review metrics. This is the global retention/completion contract; `workflow/short-form-hook-plan.json` is only the vertical short-form specialization.
   - When the global `ui-ux-pro-max` skill is installed at `$CODEX_HOME/skills/ui-ux-pro-max` or `~/.codex/skills/ui-ux-pro-max`, run it as an optional local design-intelligence preflight. Write `workflow/ui-ux-pro-max-design-intelligence.json` for every run and `workflow/ui-ux-pro-max-design-system.md` when a design-system recommendation is available. Treat this as upstream art-direction evidence for style, palette, typography, UX, caption safety, and reduced-motion rules; do not let it replace this workflow's renderer, timing, rights, voice, image2, cover, or QC contracts.
   - Create `workflow/production-plan.json`, `workflow/material-candidate-pool.json`, and `workflow/motion-grammar-plan.json` before rendering so the run has a reviewable agent production plan, scene-level material replacement pool, and reusable MG/chart/diagram grammar.
   - `workflow/caption-style-plan.json` is mandatory for every run. It must translate platform/content context into a premium deterministic subtitle layer: style catalog, per-scene caption treatment, typography, geometry, contrast, safe area, cue motion, keyword emphasis, semantic cue types, and reject list. It must read `assets/caption-style-catalog.json`, select concrete catalog styles for scenes, and preserve one-line sequential visual subtitles without creating new TTS cuts or changing subtitle timing.
   - `workflow/typography-motion-plan.json` is mandatory for every run. It must translate all major on-screen text into licensed font stacks, type roles, aesthetic treatments, semantic text motion, readable text ownership, text presentation modes, reduced-motion guards, and per-scene renderer classes. This applies to subtitles, scene headlines, body copy, labels, UI states, metric text, quotes, annotations, motion notes, and cover-continuity opening text. It must read `assets/typography-style-catalog.json`, preserve deterministic exact text, and reject remote font loading, unlicensed font use, plain left-column text regressions, or one fixed left-text/right-card layout.
   - `workflow/quality-consistency-contract.json` is mandatory for every run. It must bind the selected template kit, premium palette policy, motion template, caption style plan, caption safe area, exact-text ownership, required artifacts, reject list, and per-scene contracts into one final-quality gate.
   - For explicit vertical short-form requests, read `references/short-form-hook-design.md` and create `workflow/short-form-hook-plan.json`. This artifact must bind the first-frame promise, 0-3s content proposition, roughly 10-second structure, Image2/aesthetic decisions, mobile caption policy, motion template route, visual-state density, and payoff loop before render. Horizontal videos may still use a strong opening hook through the normal design, motion, and visual-rhythm plans, but must not activate the vertical short-form contract unless the user asked for vertical/short-form output.
   - Palette and accent color must be derived from the brief/template/topic and recorded in design artifacts. Do not reuse one fixed accent color across unrelated templates, topics, covers, delivery pages, and rendered scenes unless the brief explicitly asks for that brand color.
   - Background color is owned by the selected `visualTheme` in `workflow/design-plan.json`, then applied by the HTML frame renderer to `body` and `.stage`. Default final videos must use light, warm, neutral, paper, glass, or product-surface backgrounds with high perceived clarity. Do not route AI, code, technical, data, tool, plugin, short-form, or hook scenes to a full black/charcoal/neon stage by default. Full-stage dark backgrounds are allowed only when the user explicitly asks for a dark/black/night/cyber/studio treatment, or when a locked scene strategy such as `explicit-dark-style`, `studio-presenter-focus`, `typed-black-white-opener`, or `locked-scene-style` records why the dark stage improves the viewer-facing idea. Ordinary hooks, `前三秒`, `3 秒钩子`, `口播`, `口播稿`, `短视频`, `AI`, `代码`, `技术`, `系统`, or `数据` alone are not enough. If dark is allowed, it must be designed as a premium scene with foreground/background material contrast, not a flat black canvas. `workflow/theme-readability-audit.json` must pass before final render can be reported.
   - Final viewer-facing frames must contain only audience-facing content. Never put page numbers, scene counters, production constraints, workflow state, execution notes, local-render labels, renderer names, QC/log labels, file paths, implementation libraries, framework names, or capability disclaimers into the video image. Phrases such as `无口播`, `强动效`, `竖屏短视频`, `本地渲染`, `local render`, `no logos`, `QC`, `renderer`, `Vue.js`, `React`, `Next.js`, `Tailwind`, `GSAP`, `Three.js`, or `delivery page` belong in workflow evidence and delivery notes, not in the final MP4 unless the user's actual viewer-facing topic explicitly teaches those exact technologies.
   - Keep consistency through shared quality bars and gates, not by making every video look the same. Each video must still vary scene roles, shot templates, camera/motion jobs, and visual metaphors according to its content.
   - Design the on-screen content as a complete presentation system: topic type, audience state, content jobs, hierarchy, display logic, visual metaphor, layout system, motion purpose, and reject list.
   - Select an HTML motion template before rendering. Read `references/html-motion-platforms.md` and `references/motion-usage-playbook.md`, choose from `templates/html-motion/motion-template-registry.json` when possible, and create `workflow/motion-template-selection.json`.
   - At intake, expose only the 2-3 capabilities relevant to the user's content instead of assuming the user knows the catalog. Read `references/capability-discovery-and-layered-motion.md` when the user asks for layered reveal, animated paths/connectors, element-by-element presentation, SVG/HTML interaction, or a less PPT-like result. Natural-language signals may activate the route; explicit `brief.layeredMotion` and `--layered-motion` override inference. Every run must write `workflow/layered-motion-plan.json`; animated paths must stay inside their semantic module below readable cards, while subtitles stay topmost and personal-IP native pages remain immutable base frames.
   - Resolve the presentation mode through four stable user-facing signals: no visual-mode signal uses the default HTML animation and may select the new layered semantic-motion flow from content; this default layered route inherits the personal-IP visual design grammar (warm paper, ink outlines, organic corners, tactile offset shadows, restrained orange/blue marker accents, and single text ownership) without activating or imitating a personal identity. `个人 IP` / `personal IP` uses verified immutable native pages with animation `off`. `个人 IP + 动画` / `personal IP + animation` must instead select `personal-ip-semantic-layers-svg-html-video`: keep the fixed persona anchor and design DNA, split persona/headline/content/path/takeaway-agent/subtitle into independently exported SVG layers, compose them with one HTML master timeline, and render the final video from that same timeline. A flattened personal-IP page used as an animation base, opaque layer slices, generic progress rails, whole-page focus boxes, cross-region connectors, or captions below content are forbidden. `白板` / `whiteboard` uses framework animation plus a behind-content whiteboard sketch/reveal layer. Explicit brief fields override natural language, and every resolved route must remain provable in workflow artifacts after child renderers finish.
   - A personal-IP animation claim must be perceptible without pixel-diff tooling and must not be satisfied by one bottom-edge progress line, one static image per subtitle cue, or a file-hash-only difference. Sample multiple monotonic foreground states within each cue, keep subtitles topmost, and write `workflow/personal-ip-layered-motion-manifest.json` with base, foreground, z-order, timeline, and reject-list evidence.
   - Select reusable scene-level style templates before rendering. Read `references/motion-style-template-design-spec.md`, load `assets/motion-style-template-library.json`, and create `workflow/motion-style-template-selection.json` so Planner, TTS Timing, Template Director, Data/Material, and Renderer/QC agents share the same content-specific layout, typography, motion, data-truth, asset-task, and QC contract.
   - When using the Remotion plugin as inspiration, read `references/remotion-motion-primitives.md` and borrow frame-driven timing, easing, transition accounting, and deterministic text-reveal primitives inside the existing `html-video` path. Do not switch the renderer to Remotion without a separate migration plan.
   - For numeric data, metrics, trends, rankings, or curve-change scenes, read `references/data-driven-motion.md` before rendering. The planner must decide whether sourced data is required, write `workflow/data-source-plan.json`, `workflow/data-series.json`, and `workflow/data-motion-plan.json` when measured values are used, and route simple time-series scenes to `data-curve-trace` before considering D3 or Manim.
   - For raw footage, reference-video matching, reusable/batch templates, professional media diagnosis, generated-video engagement prediction, whiteboard/hand-drawn reveal, Galacean-style visual effects, or external video-tool comparisons, read `references/external-capability-fusion.md` before rendering. Borrow only bounded capabilities from FFmpeg, OpenMontage, Video-Use, HyperFrames, Remotion, D3, Manim, Galacean Effects, whiteboard engines, or engagement-prediction research; keep `codex-video-workflow` as the governor and write `workflow/external-capability-fusion-plan.json` with triggers, active state, evidence paths, and rejected whole-stack adoption records.
   - When the brief explicitly asks for visual effects, particles, fireworks, 2D/3D effects, energy beams, magical UI accents, or when Planner determines that a hook/reveal/transition/spatial metaphor/payoff scene needs a premium effect layer, read `references/galacean-visual-effects.md`, load `assets/galacean-effects-capability-catalog.json`, and create `workflow/galacean-effects-plan.json`. Treat Galacean/effects-runtime as an optional visual-effects layer inside the visual motion system, not as a renderer replacement. The plan must decide which scenes use effects, which scenes reject them, exact placement/layer order, subtitle-safe regions, asset rights, deterministic fallback, and screenshot/motion QC evidence. Captions and exact viewer-facing text stay in deterministic HTML/SVG/CSS/subtitle layers.
   - When a scene asks for or benefits from whiteboard-style hand drawing, read `references/whiteboard-layered-reveal.md`. Use it only as a layered foreground reveal over the current framework background: background stays framework-owned, the whiteboard engine draws semantic foreground groups, colored components resolve above the sketch layer, and subtitles/captions remain topmost. Active runs must write `workflow/whiteboard-layered-reveal-plan.json` and prove the layer order with screenshots/QC.
   - Planner must evaluate media enrichment for every non-trivial口播/解释视频 and write `workflow/media-routing-plan.json`. In `auto` mode it should run the free-stock/provider route when practical, record provider attempts or blockers, and continue only when the missing素材 is explicitly documented. In `required` mode, missing free-commercial素材 or required Image2/Codex scene images is a QC failure.
   - For semi-auto/custom generation, run `scripts/build-semi-auto-config-html.mjs --package <output-dir>` after the preparation package exists. The page must show base parameters, all supported visual motion templates with clear animated previews, expanded premium color systems as compact selectable palette rows, all 68 caption styles as selectable rows with checkbox + real subtitle treatment preview, material source toggles, personal-IP/hand-drawn fused previews, voice choices for Chinese, English, bilingual, dialect/accent when local assets support it, plus all supported speech styles. It must also include a simplified cover design module with cover generation enabled by default, one large final/sample cover preview on the left, one right-side options panel containing the default cover toggle plus compact selectable resolution chips, and click-to-open large previews sourced from `cover/*` / `workflow/cover-size-selection.json`. Keep methodology evidence in workflow artifacts instead of rendering old decision/template/status blocks inside the configuration page, and keep simulated video previews free of page numbers, scene counters, renderer/framework/library labels, and capability labels.
   - When a brief or user request asks to enrich口播视频 with free commercial-compatible stock material, or the Planner's auto policy finds a useful素材 opportunity, enable the free-stock material engine instead of manually pasting media into scenes. The engine must derive scene queries from the口播稿/video素材稿, try configured providers in order, normalize clips with FFmpeg, write `workflow/free-stock-material-plan.json` and `workflow/free-stock-asset-ledger.json`, then insert approved clips as supporting B-roll while preserving the deterministic style, MG, captions, and layout QC gates. Supported adapters are `local-authorized`, `direct-url`, no-key `nasa`, API-key `pexels`, API-key `pixabay`, and `fixture` for local demo/smoke only. Do not treat fixture assets as publication-ready external素材.
   - When the user asks for virality, views, likes, platform engagement, or publish-readiness prediction for a generated video, read `references/engagement-prediction.md`. Treat this as a relative pre-publish diagnostic unless a current, platform-specific, locally evaluated model proves exact metric prediction. Do not claim exact views, likes, shares, or comments from the generated MP4 alone.
   - Video-Use fusion means transcript-first raw-footage editing, not renderer replacement. When authorized raw footage is present, the run must write `workflow/raw-footage-inventory.json`, `workflow/raw-transcript-index.json`, `workflow/takes-packed.md`, `workflow/word-boundary-map.json`, `workflow/edit-decision-list.json`, `workflow/cut-boundary-qc.json`, and `workflow/source-media-normalization-plan.json`. Borrow the core correctness rules: never cut inside a word, keep 30-200ms cut-edge padding, use output-timeline subtitle offsets, shift overlay PTS to its window start, apply burned subtitles last, and self-review each output cut boundary. Do not make ElevenLabs/Scribe or any cloud ASR the default; cloud transcription is explicit opt-in only.
   - When the brief explicitly involves an IP diagram route, personal IP roles, creator personas, hand-drawn knowledge cards, Agent collaboration diagrams, course/PPT/livestream page systems, creator-led teaching with a strong IP/diagram signal, or asks to borrow/use `haloshin/ip-diagram-creator`, read `references/ip-diagram-creator-integration.md` and load the vendored upstream Skill at `vendor/ip-diagram-creator/SKILL.md` plus its `references/*.md` files. Generic teaching/course/tutorial/explainer wording alone is only context and must not turn this into a global default house style. Planner may select it as a video-level native-final visual engine, use it as the primary teaching visual-planning route inside the current renderer for non-personal-IP videos, keep it as a supporting prompt/QC route, or preserve its native direct-generation route for character sheets, knowledge-card/source-plate images, and PPT-style diagram pages. Any brief that says `个人 IP`, `personal IP`, `creator persona`, or sets `personalIp` must activate `ip-diagram-creator` primary planning, native source jobs, fixed/default persona rendering, native-final page provenance, native page-count gating, and mandatory `native-final-video`. If verified native page provenance or the personal-IP native page count is missing, stop before final MP4 composition and generate or ingest native pages first; integrated HTML/SVG/CSS personal-IP imitation must not deliver the MP4. The fixed-persona chain still applies: reuse a saved user-material-library persona manifest when provided; if no user-specific persona or authorized input exists, use the default non-likeness male/female host manifests under `~/.codex/video-workflow/user-assets/personal-ip/generic-hosts/` and record `doNotClaimUserLikeness`. Default host gender must follow the actual package audio gender after audio generation/reuse/provided-audio normalization: `workflow/voice-subtitle-manifest.json.audioGender: "female"` selects the female host manifest, `"male"` selects the male host manifest. Brief-level `audioGender` / `voiceGender` is only pre-audio intent and must not override the actual generated backend voice metadata; user-provided audio must provide `brief.audioGender`, `brief.voiceGender`, or `--audio-gender` when gender is not otherwise recorded. The retired local personal-IP SVG/template fallback is forbidden for final frames: do not render `ip-persona-svg` or `template-fallback`; QC must fail if either appears, and must see `fixed-persona-manifest` evidence instead. Active video runs must write `workflow/ip-diagram-creator-plan.json`, `workflow/personal-ip-asset-registry.json`, `workflow/ip-diagram-creator-vendor-usage.json`, `workflow/ip-diagram-creator-native-jobs.json`, `workflow/native-page-count-policy.json`, and `workflow/ip-diagram-layout-audit.json` with source repo/commit/license, vendored Skill entrypoint, prompt-template availability, activation signals, `plannerDriver`, execution modes, fixed-persona user material library policy, character visual DNA, `directorPlan`, `pageCards`, role-asset policy, scene assignments, native prompts, layout non-overlap contract, prompt contract, QA checklist, user choices for personal IP and hand-drawn image animation, stable full-screen page contract, and selected visual-engine ownership. For standalone personal-IP image/page requests, write a multi-page source set with vertical `workflow/vertical-personal-ip-image-*` artifacts by default or horizontal `workflow/horizontal-personal-ip-image-*` artifacts with `--aspect 16:9` using `scripts/plan-vertical-personal-ip-image.mjs`; the script must compute `workflow/personal-ip-image-count-plan.json` and `workflow/*-image-jobs.json` with default min 4, reasonable default max 48, and duration/cue/content-clarity-driven growth from actual audio/video seconds, subtitle cues, and 口播/内容 length. Manual `--target-image-count` and `--max-image-count` may not reduce the resolved count below the automatic duration/content/cue policy; the script must raise under-count targets or under-count max caps to `automaticResolvedTarget` / the automatic max policy by default and record `explicitTargetUnderAutomatic`, `explicitTargetRaisedToAutomatic`, `maxImageCountUnderAutomaticPolicy`, and `maxImageCountRaisedToAutomaticPolicy`. Only an explicitly degraded draft may use `--allow-under-count true`, and such output cannot be used as a final native-page video source. The page planner must resolve a saved or fixed default persona manifest from audio gender unless an explicit manifest or persona-gender override is supplied, write `workflow/context-image2-persona-page-requests.json` and `workflow/personal-ip-asset-registry.json`, include the manifest and role anchor in every page prompt/contract/QC, and fail rather than relying on a free-text persona description. If Codex `image_gen` bitmaps are produced, generate every page through `workflow/context-image2-persona-page-requests.json` with the same fixed persona context images attached; the required context image must be a clean presenter-only `main-anchor`, not a role sheet, design board, whiteboard example, or multi-view page. Then ingest the whole set with `--source-images` plus `--persona-reference-bound true` and verify count plus requested orientation; `--source-images` must be generated page outputs only and must fail if any path or file hash matches `main-anchor`, `sourceGeneratedImage`, role/spec sheets, style boards, or other persona reference assets. Ingested source pages without `--persona-reference-bound true` fail final QC and the planner exits nonzero unless explicitly marked draft with `--allow-draft-output true`. Prompt-only page packages must stay pending and cannot be final native pages. A single image is a draft-only exception, not a valid personal-IP final source set. Reuse a saved personal-IP manifest before generating a new user-specific presenter, and never store private role assets in the public Skill package. Do not make it a global default house style or public storage for private role assets. When `native-final-video` is selected, `ip-diagram-creator` owns the final visible frame design and our workflow only wraps timing, voice/subtitles, export, cover/package, rights, and QC; native generated page images must satisfy the source `workflow/personal-ip-image-count-plan.json` automatic policy as well as the personal-IP min/max page-count range, be full-screen fixed base frames with no border/card wrapper, no shadow/inset, and no per-cue crop/zoom/pan/y-offset. Use `scripts/render-ip-diagram-native-pages.mjs` when official/native page images already exist and the framework should add audio, subtitles, QC, and optional foreground-only hand-drawn animation; it must exit nonzero when final QC fails unless an explicit draft/degraded flag is provided.
   - Personal-IP animation exception and newer route contract: the preceding native-final bitmap requirements apply to plain/static personal IP only. When personal IP and animation are both requested, keep the same fixed/default persona, character DNA, rights, and planner evidence, but use `scripts/export-personal-ip-semantic-layered-video.mjs`; require independently owned SVG layers, `index.html`, `personal-ip-layered.svg`, `workflow/personal-ip-semantic-layer-manifest.json`, `logs/qc.json`, and a video rendered from the same HTML master timeline. A flattened native page must be rejected as this route's animation base. Verify with `scripts/self-test-personal-ip-semantic-layered-export.mjs` before delivery.
   - Personal-IP semantic-layer aspect contract: this route must own two native layouts selected from the resolved brief canvas. `9:16` renders a 1080x1920 vertical-stacked semantic board; `16:9` renders a 1920x1080 horizontal two-module semantic board. The horizontal version must be separately laid out and must not be produced by cropping, squeezing, scaling, letterboxing, or reusing vertical coordinates. `workflow/personal-ip-semantic-layer-manifest.json.aspectAdaptation` must record both supported layouts, the selected layout, and `cropOrSqueezeFallbackForbidden: true`; run `scripts/self-test-personal-ip-semantic-layered-export.mjs` for both aspect ratios before delivery.
   - Personal-IP routing uses the stricter activation contract in `references/ip-diagram-creator-integration.md`: normalize boolean/string/object fields, strip negated phrases before matching, and never treat generic knowledge-card/tutorial/course/PPT/explainer content as personal IP. Main workflow must write `workflow/visual-series-routing-plan.json`; explicit non-negated personal IP wins over all non-persona series, while other scenes map by content shape to the nine catalog series and retain matched/rejected evidence. Candidate series stay `recommend-only` and `text-safe` until approved, so Planner may prepare the corresponding generation route without silently inserting an unapproved bitmap into final composition.
   - Route aesthetic work deliberately: `$design`/`DESIGN.md` for durable taste rules, `ui-ux-pro-max` for local product/style/color/typography/UX design-system recommendations, `awesome-design-md` for mature style contracts, Creative Production moodboard/scene/shot explorers for directions, `generative-polish` for premium image finish, and visual-verdict style screenshot review for iteration.
   - Read `references/image-generation-routing.md` before changing image-provider logic. Use image2-compatible prompts for original still assets. Canonical generated-image rule: in Codex App final-quality runs, every workflow-generated bitmap must be produced through Codex Context Image2 / built-in `image_gen`, recorded with provider `codex-context-image2` and tool `image_gen` in the request manifest, copied into the project, and ingested before it can satisfy QC. This applies to covers, target-ratio cover variants, scene stills, Image2 visual-series pages, personal-IP source pages, role/action sheets, knowledge-card/source plates, and any generated poster/thumbnail asset. Deterministic HTML/SVG/CSS, charts, MG layers, subtitles, and exact text remain framework-owned design layers, but they are not substitutes for a required generated bitmap. Local SVG/HTML rasters, stock/provider media, direct API images, user-imported files, or prompt-only `image2-dryrun` packages may be used only as deterministic layers, external materials, explicit draft/review evidence, or non-Codex opt-in automation; they must not be labeled as workflow-generated Image2 assets or satisfy final generated-image provenance. Cover generation is Image 2-first by default: use a project-bound GPT Image 2/Codex `image_gen` bitmap as the complete cover whenever available, with the main Chinese title, subtitle sticker, method/support line, action badge, topic-specific visual subject, lighting, and material finish integrated inside the bitmap. Deterministic SVG/HTML Chinese text overlays are fallback/text-repair layers only, not the normal cover engine. Codex app runs may use built-in `image_gen` assets via `--image-source codex-builtin --codex-image-assets-dir <dir>` without `OPENAI_API_KEY`; direct API runs use `--image-source image2` only when explicitly requested and cannot replace Codex Context Image2 as the default Skill path. When an Image2 request manifest includes `parallelGenerationPolicy.allowed: true`, execute independent requests with bounded concurrency by default (`CODEX_VIDEO_IMAGE2_CONCURRENCY`, default 2) while preserving each request id, prompt path, context image set, and expected output mapping; never use unbounded concurrency or parallelize requests that depend on another generated bitmap. Scene-level generated images remain optional and must be traceable to the current scene's narration/headline through `workflow/visual-relevance-audit.json`; generic decorative inserts are a QC failure. For science, education, workflow, mechanism, or high-information-density explainer scenes, the planner must evaluate the Image2 explainer-board route before rejecting generated images merely because the scene is abstract; any approved board is a text-safe storyboard/source plate under deterministic HTML/SVG captions, labels, and numbers, not a finished PPT slide. The approved default for this route is `image2-explainer-board-v1`, recorded in `assets/visual-presets/image2-explainer-board-v1.json` and each active run's `workflow/visual-preset-lock.json`.
   - When writing Image 2 / Codex `image_gen` prompts, read `references/gpt-image-2-prompt-library.md` and borrow only prompt-method patterns from the YouMind GPT Image 2 prompt collection: use-case/style/subject taxonomy, structured fields, dynamic slots, composition/material/lighting language, and negative constraints. Do not copy community prompts, creator styles, brands, IP, logos, or exact layouts into video packages.
   - When the planner wants high-quality generated pages as full-screen video base frames for non-personal-IP scenes (knowledge cards, guide boards, relationship maps, atlas cards, editorial hooks, mood pages, interface plates, consistency grids), route through the visual series catalog `assets/gpt-image-2-visual-series-catalog.json` per `references/gpt-image-2-visual-series.md`: plan page sets with `scripts/plan-image2-series-pages.mjs` (per-page prompts, image-count plan, series style lock, QC, provenance manifest), record the chosen `seriesId` in the scene's `visualAssetDecision`, keep `text-safe` as the default text policy, and treat per-series `integrated-chinese` as an opt-in that requires an exact whitelist plus a proofread gate. This route borrows the personal-IP native-page design philosophy but never modifies the personal-IP route, persona manifests, or their QC gates.
   - Read `references/visual-rhythm-rules.md` before changing scene timing or image insertion. Abstract/process scenes should use deterministic MG/chart/state/typography motion instead of forced bitmaps. Every approved generated-image scene must have visible crop/focus/scan changes inside the narration segment; do not hold one unchanged image through a long口播 segment.
   - Reject pure text-card/PPT output for final videos unless the user explicitly asks for slides.
   - Do not remove motion to save render time. Every final video must have scene-level animation or camera/element movement that follows the selected motion language.
   - Do not invent motion randomly. Motion must come from the selected template or a documented custom template, and must serve a named job: reveal, compare, pressure, trace, accumulate, resolve, inspect, or transform.
   - UI composition must follow the selected template kit, safe zones, typography hierarchy, color rules, inserted visual assets, and caption layout from the design artifacts.
   - When a scene uses cards, panels, generated images, charts, proof blocks, or UI-like surfaces with inserted text, the layout must preserve both display completeness and sentence fluency. Do not fix space pressure by clipping, ellipsizing, hiding overflow, splitting a complete sentence into incomplete display fragments, or shortening copy only to fit a card. Reduce density, scale typography, reflow, split the scene, or use sequential one-line cues instead.
5. Plan voice by video type before TTS:
   - For every Chinese generated-TTS run, read `references/chinese-pronunciation-control.md`. Analyze the complete finalized `script/narration-spoken.txt` before segmentation or synthesis, resolve and audit every blocking polyphonic occurrence, and write `workflow/chinese-pronunciation-preflight.json` plus `workflow/effective-pronunciation-plan.json`. Never begin TTS while the strict preflight has unresolved occurrences.
   - Create `workflow/voice-direction.json` and `script/narration-spoken.txt`.
   - Use the supplied audio file exactly as the narration source when `--provided-audio`, `--audio`, `brief.providedAudio`, or `brief.audioPath` is present. The supplied file must be voice-only: never pass a workflow-produced `mix.m4a` back into this branch, because it may already contain the delivery mix. In this branch, the workflow must copy the source into the package, apply requested trim start/end, keep the voice asset unprocessed until the single final audio delivery normalization pass, mark `workflow/voice-subtitle-manifest.json` as `voiceBackend: "provided_audio"` with `providedAudio.authorizedByUser: true`, and derive final duration from the supplied audio rather than regenerating TTS.
   - The default narration route must not add an audible synthetic sine-pad or placeholder music bed. `assets/mix.m4a` is a voice-only delivery derivative; if music is explicitly requested, it must be a separate authorized track, mixed exactly once, recorded in the manifest, and included in audio QC. Never stack the narration mix chain and the final MP4 normalization chain; the final delivery chain owns the one loudness pass.
   - When no supplied audio file is present, generate narration through the skill voice chain: `--voice-backend auto` tries local `cosyvoice_local` first and then `melotts_local`; explicit `cosyvoice_local` or `melotts_local` limits the run to that backend. Do not use macOS `say` or any cloud/clone voice path unless the user explicitly requests a degraded fallback and the manifest records that limitation.
   - If the user asks for口语化/conversational speech, add breathing pauses only after complete sentences or complete semantic beats.
   - Treat comma-like punctuation (`，`, `,`, `、`) as short in-clause pauses, not line-break or sentence-end pauses. If an explicit pause duration is inserted, use `0.5s`; leave sentence-ending punctuation (`。`, `！`, `？`, `!`, `?`) on the backend/default pause.
   - Do not insert pauses between subject and predicate, verb and object, number and unit, or setup and required answer.
   - Use different voice rules for tutorials, explainers, story narration, news analysis, product demos, and documentary narration.
   - Use normal audible口播 loudness. Prefer a clearly present voice mix over a quiet narration; final QC must include `volumedetect` and fail if the voice is not comfortably audible at normal playback volume.
   - Control dynamics, not only average volume. Apply compression/dynamic normalization/limiting/loudness normalization when narration sounds uneven, and record the filter chain.
   - For MeloTTS Chinese narration, use the uppercase language code `ZH`, CPU device by default, and a Chinese default speed around `0.95` unless a different speed is intentionally recorded in the manifest.
   - Apply controlled MeloTTS phrases to both `pypinyin.load_phrases_dict()` and `jieba.add_word()` before inference, then verify the real MeloTTS G2P result. When a Chinese pronunciation plan requires controlled readings, lock `auto` to `melotts_local`; do not fall back to an unverified backend if MeloTTS fails. Keep pinyin annotations audit-only so narration, subtitles, and visible scene text remain unchanged.
6. Build one authoritative timecode plan before final render:
   - Create `workflow/sync-timecode-plan.json` after narration exists. It must define how spoken audio, subtitles, scene changes, visual rhythm events, progress indicators, and cover-derived opening visuals share timing.
   - Final video duration must be derived from actual generated narration duration, not from a fixed sample cap or stale pre-TTS estimate. By default, the MP4 starts directly on the first content scene and narration starts at 0s; only render an opening cover into the video when the user explicitly asks for it, and never add a silent title-card duration unless explicitly requested.
   - Split `script/narration-spoken.txt` into `script/frame-narration-segments.json` before TTS. Each visual frame must own exactly one spoken segment, and concatenating those segments must preserve the spoken narration exactly except whitespace.
   - Split each frame segment into sentence-complete or semantic-beat subtitle cues in `script/subtitle-cue-narration-segments.json` before TTS. Each cue must own one local TTS segment, and concatenating all spoken cue text must preserve `script/narration-spoken.txt` exactly except whitespace. Visual line wrapping belongs in caption layout, not in TTS segmentation; never split a word or sentence just because a subtitle line is too long. If a complete audio cue needs multiple visual subtitle lines, display those lines sequentially as one-line visual cues; never show the wrapped lines at the same time.
   - Generate TTS from those cue-bound segments and write actual segment `start`, `end`, and `durationSeconds` into `workflow/voice-subtitle-manifest.json`. Main scene durations are the sum of their cue durations; subtitles and burned-in caption timing must be copied from actual cue segment timings.
   - Prefer forced alignment or ASR-assisted alignment when word-level or phoneme-level timing is required. At minimum, scene and subtitle cue timing must come from the same local TTS source, not separate fixed-duration estimates.
   - Do not advance main visual chapters by equal scene duration when the narration is not equal-duration. Fixed-duration scene cuts over long narration are a sync failure.
   - Do not compute main visual timing by dividing total narration duration across frames. Total-duration alignment alone is insufficient; each scene cut must match its corresponding TTS segment.
   - Do not pad a short narration track with silence to satisfy an estimated duration. If TTS emits multiple chunks for one segment, concatenate every ordered chunk before calculating final timing.
   - Verify stream duration delta, `frameAudioTimingBound`, and semantic timing evidence before claiming audio/video sync.
7. Render with the lightest suitable path:
   - Recommended: `html-video` content graph plus per-frame designed HTML after storyboard, then local `cosyvoice_local -> melotts_local` narration, burned-in or otherwise visibly verified subtitles, and QC packaging.
   - Fallback: existing `.agents/skills/video-maker/scripts/video-workflow.mjs` or `.agents/skills/video-maker/scripts/video-maker.mjs --renderer ffmpeg`, only when `html-video` is unavailable or fails after the recorded retry policy. The fallback must be explicitly allowed with `--allow-degraded-renderer` and reported as a draft/degraded renderer; otherwise stop with render failure evidence instead of producing a lower-quality final.
   - For second-pass edits, treat the previous package as a node graph, not as disposable output. Pass `--base-project <previous-output-dir>` or reuse the same output directory so unchanged covers, voice cache, visual assets, timing evidence, and render logs remain available. Write `workflow/edit-lineage.json` and apply the requested change on top of the previous project instead of recreating the whole package.
   - For localized scene-level visual repairs where narration, subtitle text, and timing do not change, run `scripts/incremental-video-edit.mjs --base <previous-output-dir> --out <revision-output-dir> --scene-id <scene-id> --label <review-label>`. This executor copies the previous package, preserves `renders/final.base.mp4`, modifies only the target scene window, reuses planner/TTS/audio/non-target scenes, fuses a new `renders/final.mp4`, and writes incremental lineage/timing/QC evidence.
   - Incremental repair packages must write `workflow/incremental-edit-lineage.json`, `workflow/incremental-timing-summary.json`, `logs/incremental-qc.json`, `logs/incremental-ffprobe.json`, `logs/incremental-blackdetect.log`, `logs/incremental-volumedetect.log`, `screenshots/incremental-base-target-scene.png`, and `screenshots/incremental-patched-target-scene.png`. A copied full-run `logs/qc.json` alone is stale evidence and does not prove the repair.
   - If the requested change modifies narration text, subtitle cue text, cue duration, scene boundaries, voice settings, music timing, or rights-sensitive assets, do not claim full reuse. Rebuild the affected cue/scene nodes and their downstream sync/render/QC artifacts, or fall back to the full workflow until a narrower executor exists.
8. Design covers before final packaging:
   - Create `workflow/cover-design.json` and `cover/*` outputs from one truthful content promise, then adapt the cover strategy per platform because YouTube/Bilibili list cards, Douyin/TikTok/Kuaishou profile/search covers, Instagram Reels grids, square feed cards, and video-internal frames have different click surfaces.
   - Cover strategy must be derived from both the user-provided `title` and the spoken narration/script. Extract the video's click motivation, one clear promise, a visual proof subject, the curiosity gap, and the content-truth payoff before writing prompts or pixels.
   - Infer the current content category before rendering. Default category presets include knowledge/tutorial, story/entertainment, review/product, news/analysis, and vlog/lifestyle; each category changes copy density, emotional signal, visual proof, and acceptable information complexity.
   - When the topic/category is unfamiliar, trend-sensitive, or explicitly requested, research current platform/category competitor covers and official platform guidance before prompt generation. Record the sources and observed click logic, but never copy a creator's layout, assets, likeness, brand treatment, or exact expression.
   - When the brief has a `title`, derive the content truth from that title description after stripping chapter/course prefixes such as `第 03 章：`; the visible cover hook should complement the title with a click-oriented pain/result/contrast promise, not simply repeat the full title as a PPT heading.
   - Write `workflow/cover-image2-prompts.json` and `workflow/context-image2-cover-requests.json` for every cover package. The core cover engine must own the full cover logic first: title/script click strategy, category, platform variants, exact text whitelist, prompt method, target dimensions, pending/upload-ready state, and QC. Context Image2 / Codex built-in `image_gen` then renders the requested native-ratio bitmap from those core prompts; manual/ad hoc image prompts must not bypass the core cover logic.
   - Cover generation is an independent platform-cover agent lane. Once the locked title/script/design plan exists, invoke the system `imagegen` Skill with Context Image2 / built-in `image_gen` in parallel with TTS, native-page assembly, and MP4 composition. A pending standalone platform cover does not invalidate an already rendered MP4, but it must make the workflow terminal result and `logs/qc.json.pass` false until the bitmap is inspected and ingested with `scripts/ingest-codex-image2-cover-target.mjs`; expose `videoPass` separately so the retained video result is not confused with publishing completion. A local SVG/HTML preview, video-internal opening cover, or request manifest alone is never enough.
   - `workflow/context-image2-cover-requests.json` is the handoff contract for Context Image2. For an ordinary single-platform run it must request only the current primary upload target inferred from the video canvas (`youtube-1280x720` for horizontal, `vertical-1080x1920` for vertical). Request every platform target only when the brief explicitly sets `coverAllPlatforms:true`, `generateAllPlatformCovers:true`, or a `coverTargetIds`/`coverTargets` list. It must point to prompt files under `prompts/context-image2-covers/`, name provider `codex-context-image2`, tool `image_gen`, bind `generationContract.skill: system-imagegen` and `executionMode: built-in-image_gen`, and include a bounded `parallelGenerationPolicy` for explicitly requested independent targets.
   - Every cover request prompt must follow the system `imagegen` structured prompt schema: `Use case`, `Asset type`, native target, goal, role-labelled input images, subject, scene/backdrop, style/medium, composition/framing, lighting/mood, color palette, `Text (verbatim)`, constraints, content binding, and avoid list. Keep the production prompt compact (normally <=4500 characters), include only viewer-relevant generation instructions, and never leak internal methodology labels or planning commentary. When a personal-IP presenter is required, attach the resolved saved/default persona `main-anchor` as `inputImages[{role:"main-anchor", path:...}]`; a free-text identity description does not establish character consistency.
   - Reusing the same output directory must preserve any completed, inspected cover whose target dimensions, prompt, and role-labelled input-image contract are unchanged. Rebuild review rasters if necessary, then restore the inspected bitmap through the canonical ingest script so `context-image2-cover-requests.json`, `cover-size-selection.json`, `cover-image2-qc.json`, `cover-design.json`, and `最终成品/` remain synchronized. Never reset a matching completed request to pending or overwrite its pixels during a video/cover rerun.
   - “Cover” in this workflow means the standalone thumbnail image uploaded in the platform publishing form after the MP4 is finished. It is not the first video frame, an in-video title card, or a review raster. In Codex App, the agent must invoke the installed `imagegen` Skill / built-in Context Image2 `image_gen` tool for the primary platform submission target; merely writing `workflow/context-image2-cover-requests.json`, exporting SVG/PNG review files, or deriving a cover from the opening page does not count as generation.
   - Cover design and video generation are separate lanes. Start the core cover lane from the locked title/script/design plan before TTS, native-page assembly, or personal-IP native-final blocking checks. If the video lane blocks because verified personal-IP native pages are missing, still complete the cover lane and write `workflow/cover-parallel-execution.json`; do not skip `workflow/cover-design.json` or replace it with a native-page review cover.
   - Personal-IP/native-final routes are not exempt from the cover contract. `scripts/render-ip-diagram-native-pages.mjs` must write `workflow/cover-design.json`, `workflow/cover-size-selection.json`, `workflow/context-image2-cover-requests.json`, `prompts/context-image2-covers/*.txt`, `cover/*`, and topic-scoped `最终成品/评审级封面-非上传终版/` review covers before the package can be called complete. Missing native upload-ready covers must remain pending for Context Image2 rather than being silently omitted.
   - `workflow/cover-image2-prompts.json` must contain GPT Image 2 compatible prompts for the active platform strategy variants plus the resolution preset list. Cover copy is generated automatically from the user title plus narration/script, then rendered as integrated Image 2 thumbnail typography: main title, subtitle/method point, action badge, and any supporting microcopy named in the prompt. Logos, platform UI, random numbers, and unapproved extra text remain prohibited.
   - For knowledge/tutorial, creator-methodology, AI/tool/workflow, video-operation, and short-video profile cover topics, apply the `high-click-knowledge-cover-v1` contract before prompt generation: derive topic, audience, selling point, and 4-8 character dominant Chinese title from the title plus口播稿; compose a native-ratio B站/YouTube or vertical short-video high-click cover with an original knowledge presenter/personal-IP speaker or strong proof subject, oversized Chinese title, before-vs-after proof module when truthful, growth/warning/action signal, strict visible-text whitelist, 3-4 dominant colors, and hard negatives against cheap marketing posters, PPT cards, workflow/configuration mocks, platform logos, watermarks, QR codes, random text, and untruthful clickbait. Knowledge presenter faces default to focused, confident, clear, approachable, or lightly surprised expressions; do not repeatedly generate frowning, angry, anxious, scolding, exaggerated grimace, or furrowed-brow faces unless the title/script explicitly needs that emotion.
   - The default cover renderer is `image2-integrated-typography-cover`: Context Image2 / Codex built-in `image_gen` owns the finished thumbnail art and typography together, including visual subject, mood, texture, depth, metaphor, title hierarchy, subtitle sticker, and badge. Local SVG/HTML owns export sizing and may perform fallback/text-repair only when the integrated bitmap is absent or rejected. The old decorative/title-card SVG cover is not an acceptable default path and may only appear as an explicitly recorded degraded fallback when no bitmap asset is available.
   - Cover composition must be selected from the content, not copied from one reference. The reference-style `problem-to-proof transformation cover` is one available template for click-improvement or problem/solution topics, but other topics should use fitter layouts such as method-roadmap, misdirection-reveal contrast, ledger-payoff reveal, character-pressure, or before-after craft. Record the selected template, reason, blueprint, and UI quality bars in `workflow/cover-design.json`.
   - Use `--cover-only` when the user wants an independent cover package before rendering the video. This must still write the click logic, video-internal cover, resolution exports, and `delivery-manifest.json`.
   - Default standalone resolution presets include horizontal `3840x2160`, `1920x1080`, `1280x720`, generic horizontal `1600x1200` / `4:3`, Bilibili common `1146x717`, vertical `1080x1920`, vertical/profile `1080x1440` / `3:4`, Instagram Reels `420x654`, and square `1200x1200` / `1:1`.
   - The semi-auto/configuration page must expose every cover target above plus the video-internal opening cover as visible reviewable examples; cover examples may use review-only local rasters, but the page must label upload readiness and must not hide missing native target-ratio Image 2 assets.
   - Do not satisfy those presets by hard-cropping a finished Image 2 cover or by placing it in a visible letterbox/matte frame. Final upload-ready exports require true target-ratio Image 2/Codex assets for `4:3`, `3:4`, Reels, Bilibili common, and square outputs. If only a non-target-ratio bitmap exists, the target must be marked `needs-native-target-ratio-image2` / `uploadReady: false`, must be listed in `最终成品/需原生重生成清单.md`, and must not be copied into the final delivery directory. The correct fix is to regenerate a native target-ratio cover through Codex built-in Image 2 or the explicit API route, not to hide the mismatch with top/bottom or left/right blurred bands, white/cream frames, outlines, matte borders, hard crops, layered duplicate artwork, or synthetic side panels. If the current package has no native Image 2/Codex asset for a missing `4:3` / `3:4` target and the user explicitly asks for a preview, the workflow may create a clearly marked `review-only-local-target-ratio-recomposition` target-ratio preview under `封面预览-非上传终版/` only when `COVER_LOCAL_RECOMPOSITION_PREVIEW=1` is set: it must be laid out natively for the target canvas, must have no blank/blur/matte/crop/layered-duplicate artifacts, must remain excluded from `最终成品/`, and must remain distinguishable from true Image 2 native output in `workflow/cover-size-selection.json`.
   - To complete pending target-ratio covers in Codex App, read `workflow/context-image2-cover-requests.json`, invoke the installed `imagegen` Skill / Context Image2 built-in `image_gen` tool for each missing native-ratio request using the manifest's bounded `parallelGenerationPolicy`, visually inspect the bitmap, then ingest it with `scripts/ingest-codex-image2-cover-target.mjs --topic <topic-dir> --target <target-id> --source <codex-imagegen-png> --inspection-status passed-human-or-vision-review`. This command verifies the source ratio, marks the exact canonical request `completed`, records `provider: codex-context-image2` and `tool: image_gen`, copies exact platform PNG/JPG exports into the topic-scoped `最终成品/` upload folder, moves the target from `pendingNativeTargetRatioPrompts` to `fulfilledNativeTargetRatioExports`, and may mark `uploadReady: true` only for the real Codex Image 2 bitmap. `primaryPlatformUploadCoverReady` is separate from `allRequestedPlatformUploadCoversReady`: one inspected primary upload cover can complete the single-platform publishing package, but it must not falsely mark every requested platform size complete. The video-production lane may finish before this state is reached; use `scripts/validate-platform-submission-cover.mjs --out <topic-dir>` as the independent publishing-cover gate. The explicit API alternative is `scripts/generate-cover-targets-image2.mjs --root <batch-root-or-topic-root> --concurrency 2` after `OPENAI_API_KEY` is available; it may raise concurrency through `CODEX_VIDEO_IMAGE2_CONCURRENCY` only when rate limits and local resources allow. Neither route may draw local substitute covers or mark local recompositions as upload-ready. A native-final personal-IP package with only review-grade cover files and pending Context Image2 requests is incomplete for publishing: QC must expose `coverNativeImage2Ready:false`, while the independently rendered MP4 may still be retained as a video-production result.
   - For batch cover completion, the generation scope is always the current missing set: `workflow/cover-size-selection.json.entries[*].uploadReady !== true`, plus any standard target declared by `workflow/cover-design.json.resolutionPresets` or `workflow/cover-image2-prompts.json.pendingNativeTargetRatioPrompts` but absent from `entries`. Do not regenerate dimensions that are already `uploadReady:true` and `image2NativeTargetRatioReady:true`. A batch is complete only when `scripts/build-cover-size-selection-index.mjs --root <batch-root>` passes without `--allow-pending-native-targets` and every topic reports `pendingNativeTargetCount: 0`.
   - For batch delivery, provide Chinese-readable size labels or an index grouped by aspect ratio, so the user can choose between `横版16比9`, `横版4比3`, `竖版9比16`, `竖版3比4`, `方形1比1`, and platform-specific crops without reading internal target ids.
   - Every cover package must write one topic-scoped user-facing `最终成品/` directory plus `workflow/cover-size-selection.json`. `最终成品/` is the only place users should choose/upload/cut from for that topic, and it must group files by Chinese aspect/platform folders such as `横版16比9/`, `横版4比3/`, `竖版9比16/`, `竖版3比4/`, and `方形1比1/`. Root-level topic copies and old `按尺寸选择/` copies must be pruned after packaging. Missing native target-ratio outputs must go to that topic's `最终成品/需原生重生成清单.md`; optional local ratio previews must go only to `封面预览-非上传终版/` and may never be counted as upload-ready. When `4:3`, `3:4`, square, Reels, or Bilibili common covers are missing native Image2/Codex bitmaps, do not write target-size review-grade drafts under `最终成品/评审级封面-非上传终版/`; leave those targets pending for Context Image2 so a cropped/recomposed local file cannot be mistaken for the real 4:3 final. For multi-topic batches, run `scripts/build-cover-size-selection-index.mjs --root <batch-root>` to rebuild each topic's own `最终成品/` and generate a lightweight `_封面总索引/` with links only; do not duplicate all final images into the batch root. This batch command must fail by default when any native target-ratio cover remains pending. Use `--allow-pending-native-targets` only for an explicitly incomplete review package; such output cannot be reported as final/all-size upload-ready.
   - Export SVG sources plus PNG/JPG raster covers for upload/review. Record `sharedContentPromiseMultiPlatformVariants: true`, `platformSpecificDesignsGenerated: true`, the inferred `contentCategoryStrategy`, `platformCoverStrategies`, each preset's dimensions, ratio, byte size, and size-limit pass/fail in `workflow/cover-design.json`; user-facing cover copies must live under the topic-scoped `最终成品/` directory, not as many title-named files scattered in the topic root.
   - The video-internal opening cover is a separate target and must keep the final MP4 aspect ratio. Do not put a vertical, square, or other upload crop inside a `16:9` video unless the whole video is that aspect.
   - Platform cover assets should be exported under `cover/*` by default and used as upload/thumbnail assets. Do not render the cover as a separate MP4 segment unless the user explicitly asks for an in-video opening cover; in default runs, QC must prove the MP4 starts directly on the first content scene.
   - Cover design must express the video's core click promise, curiosity gap, visual subject, emotional signal, and mobile-readable hook. It must look like a professional novel/video cover, not a title card, PPT slide, platform/spec mock, or visible workflow label.
   - The opening seconds should visually honor the cover promise so the thumbnail and content feel continuous.
9. Write a static delivery review page:
   - Create `delivery.html` in the output directory for every complete video package and cover-only package.
   - The page must be self-contained and pre-render local data into HTML instead of relying on `fetch()` from `file://`.
   - The page language must follow the user/brief language. Chinese briefs produce a Chinese HTML page; English briefs produce an English HTML page. This applies to headings, buttons, descriptions, helper text, and material labels.
   - It must show the final video when present, covers/title images, narration, spoken script, storyboard, subtitles, QC summary, workflow evidence, and grouped material links with relative and absolute local file paths.
   - It must include a one-click-copy title, a promotional video-platform-style description that highlights the current generated video's value, and a one-click-copy description.
   - Generate `delivery-service.mjs` and open the page through the local service by default. File/path buttons must call the system file manager (`open`, `explorer`, or `xdg-open`) through the service instead of navigating to `file://` in the browser; direct `file://` viewing may fall back to copying the open command.
   - Open `delivery.html` in the default browser after generation unless `--no-open-delivery-page` is passed. In the same completion step, also open the whole output package directory in the system file manager so the user can browse every artifact (final MP4, `最终成品/` covers, scripts, logs, evidence) directly. The `--no-open-delivery-page` / CI opt-out suppresses both the page and the folder open.
10. Verify before claiming completion: run `scripts/validate-platform-submission-cover.mjs --out <output-dir>` so the primary standalone platform upload cover proves a completed Context Image2/`image_gen` request, inspected native-ratio bitmap, and real file in `最终成品/`; add `--require-all-platform-covers` only when claiming the entire multi-platform cover suite is complete. Also verify ffprobe metadata, stream duration delta, per-frame TTS segment timing binding, sync-timecode plan, page-decision contract, retention-structure contract, typography-motion plan, caption-style plan, image-generation strategy, visual rhythm plan, external-capability fusion plan, generated-image purpose-fit decision, generated-visual design-layer preservation, visual asset visible-text audit, frame layout overlap audit, frame copy integrity audit, `volumedetect`, `silencedetect`, blackdetect, screenshots, subtitle/audio existence, visible one-line sequential subtitles, direct first-scene start by default, zero-second narration start, cover artifacts, cover Image 2 prompts, platform cover PNG/JPG byte-size evidence, title-named MP4 copy, voice direction, rights record, scorecard, package manifest, delivery page, command log, quality-consistency contract, production plan, material candidate pool, motion grammar plan, and screenshot/motion evidence. If Galacean visual effects are active, additionally verify `workflow/galacean-effects-plan.json`, visual-asset provenance, pre/peak/post effect screenshots, caption-safe layer order, deterministic exact-text ownership, fallback, and `logs/qc.json` booleans `galaceanEffectsPlanPresent`, `galaceanEffectsCaptionSafe`, `galaceanEffectsRightsRecorded`, and `galaceanEffectsMotionEvidencePresent`. `logs/qc.json` must pass `retentionStructureContractPresent`, `firstFrameRetentionPromisePresent`, `firstThirtySecondContractPresent`, `evidenceCadencePlanned`, and `progressAndPayoffPlanned` for every final-quality run. For active IP diagram routes, additionally verify `ipDiagramCreatorVendorUsagePresent`, `ipDiagramCreatorNativeJobsPresent`, and `skillUsageAccuracyAuditPass` so the run proves it loaded `vendor/ip-diagram-creator/SKILL.md` and upstream prompt templates before claiming `haloshin/ip-diagram-creator` use. For IP diagram native-final page routes, additionally verify `ipDiagramFullScreenStable`, `ipDiagramNoBorderWrapper`, `ipDiagramBaseImageStable`, `ipDiagramNoPerCueCropPanZoom`, `noVerticalBackgroundJump`, `coverArtifactsPresent`, `coverContextImage2HandoffPresent`, `coverNativeImage2Ready`, `nativePageCountSatisfiesSourceImageCountPlan`, and `allFinalPagesHaveFixedPersonaReferenceBinding`; when the native-final canvas is vertical, also verify `workflow/vertical-personal-ip-design-contract.json`, `dimensions1080x1920`, and `nativePagesGeneratedForVerticalCanvas`. For generated packages, run `scripts/validate-subtitle-cover-contract.mjs --out <output-dir> --brief <brief.json>` whenever the package includes narration, subtitles, and covers. After changing subtitle style routing, run `scripts/validate-caption-strategy-routing.mjs` to prove state/scene triggers still select the intended caption families instead of only validating that fields exist. Do not claim completion when audio is present but too quiet, the口播 starts after a silent opening cover, the MP4 starts with an unwanted cover hold, the口播 drops into long post-opening silence, page-decision contract is missing or leaves any scene without content/design/interaction/animation/ownership answers, retention-structure contract is missing or not enforced across the whole video, the first frame lacks a viewer-facing promise, the first 30 seconds lack promise/structure/proof route, evidence cadence is unplanned, progress/payoff is unplanned, typography-motion plan is missing or unenforced, remote/unlicensed fonts are used, animated text is clipped/hidden/unreadable, subtitles wrap into simultaneous multi-line display, the caption style plan is missing or unenforced, a visual subtitle line becomes a separate incomplete TTS/audio cue, headlines/body text/subtitles overlap images/charts/proof panels, any inserted deterministic visual exposes internal planning labels or ellipsized visible copy, any visible text is clipped/ellipsized/off-canvas/hidden by overflow, card copy is shortened into an incomplete sentence because space is tight, generated images are inserted without scene-level purpose/placement, generated images replace the deterministic style/MG/platform/motion-note layers, generated images collide with captions or motion overlays, a scene holds one unchanged image through a long narration segment, native IP diagram pages are rendered as inset/bordered/shadowed cards, native IP diagram static pages move/crop/zoom/pan/jump between subtitle cues, vertical native IP diagram videos are made by cropping or squeezing horizontal pages, IP diagram native-final output has no cover artifacts or no Context Image2 cover handoff, IP diagram native-final output lacks `coverNativeImage2Ready:true`, final personal-IP pages do not satisfy the source image-count plan, final personal-IP pages lack fixed-persona image/context binding evidence, Galacean/effects-runtime is used as a whole-renderer replacement, an active effect has unknown asset rights, effect placement covers subtitles/data labels/source notes/key proof text, exact Chinese claims exist only inside WebGL/canvas textures, active effects have no screenshots or motion evidence, the cover Image 2 prompts are missing, the primary standalone platform submission cover request is not completed through Context Image2/`image_gen`, the inspected native-ratio cover is absent from `最终成品/`, platform cover PNG/JPG size evidence is missing or over limit, the title-named MP4 copy is missing, the cover title does not come from the brief title description when one exists, the renderer became static, the UI no longer follows the design plan, external video tools are invoked as whole-stack replacements, whiteboard layered reveal is active but subtitles are not topmost, the framework background is replaced by an unintended white board, the whole scene is traced, or final readable text exists only as stroke line art, the external-capability fusion plan is missing or unenforced, the quality-consistency contract is missing or unenforced, the production plan or scene-level candidate/MG evidence is missing, the main visual timing drifts from narration, the delivery page is missing, or cover artifacts are missing.
   - For short-form vertical output, QC must additionally prove that the final MP4 canvas is `1080x1920`, the video-internal cover uses the final MP4 ratio, subtitles stay inside the vertical safe area, top titles/key text stay below the mobile status/navigation bar safe area, the first scene starts with a hook beat, `workflow/short-form-hook-plan.json` exists, and `logs/qc.json` passes `shortFormHookPlanPresent`, `firstFramePromiseVisible`, `contentPropositionWithin3s`, `firstThreeSecondVisualDensityOk`, `shortFormImage2AestheticDecisionPresent`, and `shortFormPayoffLoopPresent`. For vertical personal-IP/native-page output, `workflow/top-safe-area-audit.json` must exist and pass with a reserved top blank area of at least `220px`.
    - For incremental repairs, QC must additionally prove `logs/incremental-qc.json.pass: true`, duration delta `<= 0.2s`, target before/after screenshot hashes differ, the final video decodes with FFmpeg, blackdetect is clean, audio is preserved/audible when the base had audio, bitrate remains delivery-grade, and `scripts/validate-subtitle-cover-contract.mjs` still passes when the copied package includes narration, subtitles, and covers.

## Local Production Run

Run the deterministic local video workflow:

```bash
node .agents/skills/codex-video-workflow/scripts/poc-video-workflow.mjs \
  --brief .agents/skills/codex-video-workflow/assets/examples/authorized-brief.json \
  --out research/codex-video-workflow-poc/authorized-video \
  --mode recommended \
  --voice-backend auto \
  --speech-style conversational \
  --image-source image2-dryrun
```

Expected outputs:

- `renders/final.mp4`
- `final.mp4`
- `<current-video-title>.mp4` in the output root as the title-named delivery copy
- `script/narration.txt`, `script/narration-spoken.txt`, `script/frame-narration-segments.json`, `script/storyboard.md`, `script/subtitles.srt`
- `script/subtitle-cue-narration-segments.json`
- `workflow/voice-direction.json`
- `workflow/runtime-config.json`
- `workflow/generation-mode-contract.json`
- `semi-auto-config.html` and `workflow/semi-auto-config.json` when semi-auto/custom mode is active or when a configuration preview is requested
- `workflow/voice-subtitle-manifest.json` with actual per-frame TTS segment timings
- `workflow/chinese-pronunciation-preflight.json`, synthesis-ready `workflow/effective-pronunciation-plan.json`, and `workflow/pronunciation-application-verification.json` for every Chinese generated-TTS run; controlled runs must block unless the application artifact proves a positive loaded phrase count and an exact loaded-plan hash match
- `workflow/final-audio-normalization.json`
- `workflow/sync-timecode-plan.json`
- `workflow/cover-design.json`, `cover/*`
- `workflow/cover-image2-prompts.json`
- `workflow/context-image2-cover-requests.json` and `prompts/context-image2-covers/*.txt` for Context Image2 / Codex built-in `image_gen` cover generation handoff
- topic-scoped `最终成品/` cover copies and selection index as the only user-facing cover output for that topic; when no native upload-ready cover exists (default口播/`image2-dryrun`), review-grade draft covers are always written under `最终成品/评审级封面-非上传终版/` so the deliverable is never cover-less, while the aspect-named upload group folders stay native-upload-only
- `workflow/content-presentation-design.json`
- `workflow/page-decision-contract.json`
- `workflow/retention-structure-contract.json`
- `workflow/short-form-hook-plan.json` when explicit vertical short-form planning is active
- `workflow/typography-motion-plan.json`
- `workflow/caption-style-plan.json`
- `workflow/personal-ip-asset-registry.json` when a personal-IP route is active, recording the user material library manifest, create-once reuse guidance, and public Skill storage prohibition
- `workflow/ip-diagram-creator-vendor-usage.json` when an `ip-diagram-creator` route is active, proving the run loaded `vendor/ip-diagram-creator/SKILL.md`, pinned source metadata, required upstream references, and upstream prompt-template availability
- `workflow/native-page-provenance-audit.json` when `ip-diagram-creator` native-final pages are used
- `workflow/native-page-count-policy.json` when `ip-diagram-creator` native-final pages are used, enforcing both personal-IP min/max native page count and the source `workflow/personal-ip-image-count-plan.json` automatic duration/content/cue count before final delivery
- `workflow/vertical-personal-ip-design-contract.json` when `ip-diagram-creator` native-final pages are rendered to `9:16`
- `workflow/personal-ip-image-count-plan.json`, `workflow/*-personal-ip-image-image-jobs.json`, and `prompts/*-personal-ip-image-pages/*.txt` for standalone personal-IP source-page sets; vertical writes `workflow/vertical-personal-ip-image-*.json`, horizontal writes `workflow/horizontal-personal-ip-image-*.json`
- `workflow/skill-usage-accuracy-audit.json` when an external Skill/capability route is used as a claimed source of final visual or production behavior
- `workflow/theme-readability-audit.json`
- `workflow/quality-consistency-contract.json`
- `workflow/external-capability-fusion-plan.json`
- `workflow/galacean-effects-plan.json` when Galacean visual effects are active
- `workflow/whiteboard-layered-reveal-plan.json` when whiteboard layered reveal is active
- `workflow/free-stock-material-plan.json` and `workflow/free-stock-asset-ledger.json` when free-stock material enrichment is active
- `workflow/media-routing-plan.json`
- `workflow/raw-footage-inventory.json`, `workflow/raw-transcript-index.json`, `workflow/takes-packed.md`, `workflow/word-boundary-map.json`, `workflow/edit-decision-list.json`, `workflow/cut-boundary-qc.json`, and `workflow/source-media-normalization-plan.json` when authorized raw footage is active
- `workflow/production-plan.json`, `workflow/material-candidate-pool.json`, `workflow/motion-grammar-plan.json`
- `workflow/ui-ux-pro-max-design-intelligence.json` and, when available, `workflow/ui-ux-pro-max-design-system.md`
- `workflow/aesthetic-brief.json`, `workflow/aesthetic-quality-rubric.md`
- `workflow/motion-template-selection.json`
- `workflow/motion-style-template-selection.json`
- `workflow/design-plan.json`, `workflow/image-generation-strategy.json`, `workflow/image2-prompts.json`, `workflow/visual-asset-manifest.json`, `workflow/visual-relevance-audit.json`, `workflow/media-routing-plan.json`, `workflow/visual-rhythm-plan.json`
- `workflow/visual-asset-text-audit.json`
- `workflow/frame-layout-overlap-audit.json`
- `workflow/commands.json`, `workflow/tool-candidate-selection.json`, `workflow/quality-scorecard.md`
- `workflow/timing-summary.json` with stage/category durations and cache-hit evidence
- `logs/ffprobe.json`, `logs/blackdetect.log`, `logs/volumedetect.log`, `logs/silencedetect.log`, `logs/qc.json`
- Optional incremental repair evidence: `workflow/incremental-edit-lineage.json`, `workflow/incremental-timing-summary.json`, `logs/incremental-qc.json`, `logs/incremental-ffprobe.json`, `logs/incremental-blackdetect.log`, `logs/incremental-volumedetect.log`, `screenshots/incremental-base-target-scene.png`, and `screenshots/incremental-patched-target-scene.png`
- Optional semi-auto page-review evidence: `page-review.html` and page-scoped TDS edit requests when a user asks to revise one page before final composition
- `screenshots/*.png`
- `AUTHORIZATION.md`, `delivery-manifest.json`, `delivery.html`
- `delivery-service.mjs` for the local interactive review page and system file-manager open buttons

Voice defaults:

- `--voice-backend auto`: try `cosyvoice_local`, then `melotts_local`.
- `--voice-backend cosyvoice_local`: prefer CosyVoice, then MeloTTS.
- `--voice-backend melotts_local`: prefer MeloTTS, then CosyVoice.
- `--allow-say-fallback`: optional degraded fallback only; do not use for final-quality videos unless the local TTS environments are unavailable and the limitation is reported.
- Local TTS outputs are cached within the output directory and, for MeloTTS, in the shared `.video-maker/tts-cache` segment cache by backend, text, speaker/language, speed, and polyphone lexicon. Reusing the same narration across output directories can restore unchanged segments without regenerating them. The shared cache is capped by `ttsCacheMaxBytes` / `--tts-cache-max-gb` and pruned by least-recently-used segment files when it exceeds the limit.
- For faster iteration, use `--speed-profile fast` or explicitly set `--tts-workers 3 --tts-device cpu --final-audio-mode copy-video`. `--tts-workers 5` is allowed for explicit benchmark/turbo runs, but keep CosyVoice or non-CPU MeloTTS paths at 1 unless measured safe.
- `--final-audio-mode copy-video` copies the rendered video stream and only rebuilds the delivery audio track; default `reencode-video` keeps the conservative crisp-text bitrate chain for final delivery.
- Second-pass edits should use `--base-project`, the same `--out` directory, or `scripts/incremental-video-edit.mjs` for scene-level visual repairs to reuse previous covers, voice cache, visual assets, logs, and evidence where the requested change does not invalidate them.

Speech style defaults:

- `--speech-style auto`: infer from video type and brief.
- `--speech-style conversational`: oral creator-style narration with sentence-end pauses only.
- `--speech-style tutorial`: clear step-by-step pacing.
- `--speech-style explainer`: balanced knowledge explanation.
- `--speech-style story`: scene/reveal-driven narration.
- `--speech-style news`: controlled factual analysis.
- `--speech-style product`: crisp demo/benefit pacing.
- `--speech-style documentary`: slower atmospheric narration.

Image defaults:

- `--image-source image2-dryrun`: default for scene insertions and offline planning only. It writes GPT Image 2 compatible prompts and Context Image2 cover request files, but it cannot satisfy final upload-ready covers by itself. Covers must still be generated through Context Image2 / Codex built-in `image_gen` from `workflow/context-image2-cover-requests.json`, or through the explicit API route, before they can be marked final.
- `--image-source local`: insert only deterministic local SVG illustrations while still recording prompt intent.
- `--image-source codex-builtin --codex-image-assets-dir <dir>`: insert project-bound bitmap assets generated by Codex built-in `image_gen`; no `OPENAI_API_KEY` is required. For covers, generate the bitmap from `workflow/context-image2-cover-requests.json` and ingest it with `scripts/ingest-codex-image2-cover-target.mjs`; when manually providing a preferred cover image, name it with `cover`, `thumbnail`, `封面`, or `海报` so it becomes the default cover subject. Name scene files by scene id such as `01-opening-risk.png` or `opening-risk.png`.
- `--image-source image2`: call GPT Image 2 through the OpenAI Images API using `OPENAI_API_KEY`; continue with local fallback only if `ALLOW_IMAGE2_FALLBACK=1` is set and the failure is recorded.
- `--scene-image-policy off|auto|required`: controls whether scene-level Image2/Codex generated images are disabled, auto-selected as supporting material, or required as a hard gate.
- `--free-stock-policy off|auto|required`: controls whether free-commercial素材/provider search is disabled, auto-attempted with visible blockers, or required as a hard gate.

Delivery page defaults:

- `delivery.html`: localized local review page with final video playback, copyable title, promotional description, covers, scripts, QC evidence, and material links.
- `delivery-service.mjs`: local review service that lets buttons open real files/folders in the system file manager instead of browser file navigation.
- Default behavior opens `delivery.html` through the local service in the system browser after generation, and also opens the output package directory in the system file manager.
- `--no-open-delivery-page`: write the page but skip launching the browser and the project-folder open, useful for CI or batch generation.

## Reference Loading

Load only the reference files needed for the task:

- `references/candidate-matrix.md` for renderer/model/tool choices.
- `references/plugin-routing.md` for how video plugins/tools are selected by task shape and kept bounded by framework artifacts, timing, rights, and QC. Every run writes `workflow/plugin-routing-contract.json`, and `logs/qc.json` hard-gates `pluginRoutingContractPresent` and `pluginRoutingContractEnforced`, so this contract is mandatory, not optional.
- `references/methodology.md` for script/storyboard/editing method.
- `references/voice-direction.md` for video-type-specific speech style, oralization, and pause rules.
- `references/cover-design.md` for platform-aware thumbnail/cover strategy, artifacts, ratios, and quality gates.
- `references/generation-modes.md` for full-auto vs semi-auto/custom mode behavior, configuration-page requirements, page-level TDS editing, and compose gating.
- `references/content-presentation-design.md` for complete on-screen information design, hierarchy, topic-fit, and premium presentation logic.
- `references/retention-structure.md` for whole-video RETAIN planning, first-frame/first-30-second contracts, evidence cadence, progress/payoff design, and retention QC gates.
- `references/typography-motion-system.md` and `assets/typography-style-catalog.json` when the brief asks for stronger typography, font selection, text-led visuals, kinetic typography, animated titles, metric text, UI labels, quote design, or any major text presentation upgrade.
- `references/subtitle-design.md` for premium caption treatment, platform-safe subtitle geometry, one-line sequential display, cue motion, and caption-style QC gates.
- `references/creative-subtitle-strategy.md` and `assets/caption-style-catalog.json` when the brief asks for advanced subtitles, keyword emphasis, caption animation, UI-like subtitle design, bilingual caption hierarchy, or Planner-selected creative subtitle styles.
- `references/aesthetic-system.md` for design taste, skill/capability routing, and visual quality gates.
- `references/short-form-hook-design.md` for explicit Douyin/TikTok/Shorts/Reels-style vertical short-form, roughly 10-second hook structure, Image2/aesthetic decisions, mobile caption policy, and payoff-loop QC.
- `references/design-platform-planner.md` for interpreting curated design sites, UI/component galleries, app-flow references, and anti-PPT video planning rules.
- `references/ip-diagram-creator-integration.md` plus `vendor/ip-diagram-creator/SKILL.md` and `vendor/ip-diagram-creator/references/*.md` for using the vendored `haloshin/ip-diagram-creator` personal-IP role assets, upstream prompt templates, white-canvas hand-drawn visual DNA, diagram shot lists, Agent collaboration diagrams, PPT director page cards, native direct-generation jobs, stable full-screen native page rendering, optional foreground-only hand-drawn animation, layout non-overlap checks, and QA repair rules as bounded planner/prompt/direct-source/QC capabilities.
- `references/design-templates.md` for type-specific visual template kits and image2 insertion rules.
- `references/image-generation-routing.md` for image-provider/tool routing, Codex built-in `image_gen`, OpenAI Images API, Firefly/Gemini/Stability slots, and prompt constraints.
- `references/gpt-image-2-prompt-library.md` for YouMind GPT Image 2 prompt-library methods: prompt taxonomy, structured prompt fields, dynamic slots, material/lighting language, and rights-safe adaptation rules.
- `references/gpt-image-2-visual-series.md` plus `assets/gpt-image-2-visual-series-catalog.json` for planner-selectable generated visual series (knowledge cards, guide boards, relationship maps, atlas cards, hooks, mood pages, interface plates, consistency grids) that reuse the personal-IP native-page design philosophy without touching the personal-IP route, produced via `scripts/plan-image2-series-pages.mjs` and validated via `scripts/validate-image2-series-catalog.mjs`.
- `references/visual-rhythm-rules.md` for audio-bound visual event density and anti-fatigue scene rules.
- `references/motion-library-strategy.md` for scene-level open-source animation/library routing, including Anime.js, Three.js, D3, Rough.js, Lottie, Motion, Galacean visual-effects layer routing, and GSAP exception rules.
- `references/html-motion-platforms.md` and `templates/html-motion/motion-template-registry.json` for HTML/CSS/JS motion platform routing and reusable template selection.
- `references/motion-usage-playbook.md` for how to use, design, show, and verify motion templates scene by scene.
- `references/motion-style-template-design-spec.md` and `assets/motion-style-template-library.json` when the task asks for 风格模板, reusable video-page templates, content-specific layouts, code/formula/data/IP/whiteboard/cover presentation logic, or Planner-selected style templates.
- `references/remotion-motion-primitives.md` for borrowing Remotion animation/timing/transition/text rules as frame-driven motion primitives without replacing `html-video`.
- `references/data-driven-motion.md` for web/API-sourced numeric data, time-series curves, data-source ledgers, data-motion plans, and renderer routing between `data-curve-trace`, D3, and Manim.
- `references/external-capability-fusion.md` for borrowing non-math external video capabilities: FFmpeg-style media diagnostics/recovery, OpenMontage-style stage gates, Video-Use-style raw-footage editing, HyperFrames-style render inspection, Remotion-style props/templates, Galacean visual effects, whiteboard layered reveal, and reference-video alignment QC.
- `references/galacean-visual-effects.md` and `assets/galacean-effects-capability-catalog.json` when the brief asks for visual effects, particles, fireworks, 2D/3D effects, energy beams, magical UI accents, or when Planner selects a hook/reveal/transition/spatial metaphor/payoff effect layer.
- `references/whiteboard-layered-reveal.md` when scenes need hand-drawn/marker/whiteboard reveal over the current designed video background while subtitles remain topmost.
- `references/engagement-prediction.md` for generated-video virality or engagement prediction audits, including the `juanls1/TikTok-Virality-Predictor` research boundary, relative-potential artifacts, model-card requirements, and exact views/likes claim limits.
- `references/video-production-skill-increments.md` for borrowed design increments from external video-production skills: typed black-white openers, dark SaaS magic UI, and reference-video alignment QC.
- `references/quality-gates.md` for pass/fail checks.
- `references/integration-roadmap.md` for how `html-video` fits the current project.
- `references/research-sources.md` for current source URLs and what each source supports.
- `references/failure-cases.md` when renderer/toolchain failure handling matters.

## Output Contract

Final delivery must name the output directory, final MP4 path, validation evidence, and any unverified or manual-review gaps. Do not say the video is platform-ready unless licensing, AI-labeling, platform upload policy, and human editorial review have been completed.
