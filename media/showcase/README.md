# README showcase provenance

These assets are packaged specifically so the GitHub README can show real, reviewable project capabilities without depending on ignored `research/` or `output/` directories. They must be added to Git before a public README can rely on them.

## Hero

`hero/codex-video-workflow-hero.png` was generated with Codex built-in `image_gen` in `generate` mode for this repository. The request described a wide, premium editorial hero that visualizes the production chain from brief and storyboard through semantic motion, subtitles, audio, platform covers, QC, and final frames. It intentionally contains no embedded title, logo, UI copy, or unsupported product claim.

## Runtime template captures

The images under `templates/` are browser captures of the repository's actual HTML motion templates at representative progress states:

- `kinetic-editorial-explainer.png`
- `semantic-timeline-reveal.png`
- `interactive-proof-board.png`
- `data-curve-trace.png`
- `dark-saas-magic-ui.png`
- `typed-black-white-opener.png`
- `gsap-semantic-flow.png`

They document current renderable visual systems rather than speculative mockups.

## 2026 core-demo evidence

`core-demo/` is a stable subset copied from the current semi-auto run generated from `media/oral-materials/core-capability-demo-2026-brief-zh.json`.

- `semi-auto-config.html` is the actual generated configuration console, not a hand-authored README mockup.
- `config-*.png` files are Chromium captures of that console. The locale captures exercise the runtime Chinese/English switch; the catalog captures show the rendered motion, caption, color, voice, cover-logic, and platform-size interfaces.
- `motion-style-template-review.html` and `vertical-motion-style-template-review.html` contain the 32-family × 5-variant review catalog.
- `generation-mode-contract.json`, `caption-style-plan.json`, and `cover-size-selection.json` preserve the structured decisions behind the visible UI.
- `cover/*.svg` files are **review previews**, not upload-ready cover bitmaps. Their incomplete status is kept visible in the main README.

The current full-auto demo MP4 and poster live directly under `media/`; `core-demo/full-auto-contact-sheet.jpg` shows all eight scenes. The 122.1-second video lane passed the current render, stream, timing, black-frame, silence, and visible-text checks after framework-level route and overflow fixes. The run remains non-publishing-ready until every requested platform cover is generated, inspected, ingested, and revalidated.

The expanded `config-*.png` gallery is generated by `scripts/capture-readme-showcase.mjs` from the actual console. It covers Chinese and English base settings, motion, captions, colors, material routing, covers, voice/localization, per-page editing, and both horizontal and vertical motion-browser pages.

## Public workflow film

`workflow-film/codex-video-workflow.mp4` is the primary public example. It is designed as a product-facing capability film rather than a renderer catalog.

- The 59.5-second 1920×1080 H.264/AAC reel uses local MeloTTS narration.
- The opening leads with finished outputs and the promise that one brief becomes a reviewable production package.
- Four concrete motion examples show how progression, comparison, connection, and focus communicate sequence, difference, relationship, and priority.
- The real bilingual configuration console, native-ratio covers, the provenance-backed Personal-IP film, and both horizontal and vertical hand-drawn whiteboard clips are shown directly.
- All 68 caption styles run as animated mini-previews across eight user-facing families, followed by full-size kinetic and bilingual-glass examples.
- `workflow/` records the content design, motion meanings, caption coverage, whiteboard layering, timecodes, and visual-asset sources.
- `logs/qc.json`, `logs/ffprobe.json`, `logs/blackdetect.log`, and `logs/layout-audit.json` preserve the automated checks. `final-contact-sheet.png` is sampled from the final encoded video.

## Archived catalog coverage film

`capability-reel/capability-showcase.mp4` is a deterministic 38.4-second, 1920×1080 catalog reel generated by `scripts/render-capability-showcase-demos.mjs` from the committed motion and caption manifests plus the generated configuration console.

- It walks all 32 motion families and displays all five variants for each family.
- It proves union coverage for all 160 motion-style contracts, 44 configuration color systems, and 68 routable caption styles across eight groups.
- It records that the catalog is backed by six executable HTML cores but does **not** misrepresent 160 contracts as 160 independent renderers.
- It contains local MeloTTS narration and committed probe, black-frame, volume, silence, coverage, and QC logs.

This catalog film is retained as internal coverage evidence. It is not linked from the public README because its generic geometry does not explain each motion's semantic job.

## Two-page Personal-IP example

`personal-ip/example-film/personal-ip-two-page-horizontal.mp4` is a short edit of two provenance-backed native pages. `provenance.json` records the source and the generic-host / non-user-likeness boundary.

## Personal-IP short demos

`personal-ip/demos/` contains two six-second README clips copied from runs whose final delivery QC reported `pass`, `videoPass`, and `publishingReady` as true. `provenance.json` records source paths, hashes, dimensions, codec information, and claim boundaries.

- The horizontal clip proves the native 16:9 Personal-IP hand-drawn teaching-page route. Its page-local foreground whiteboard-motion plan was inactive, so the README does not claim otherwise.
- The vertical clip proves the native 9:16 Personal-IP base-page plus page-specific hand-drawn path/node foreground-motion route, with captions kept topmost.
- Both clips use the saved `generic-host-female` fallback persona and explicitly set `doNotClaimUserLikeness: true`; they are not presented as the user's likeness.

## Personal-IP and cover examples

The images under `personal-ip/` and `covers/` are unchanged outputs copied from one locally generated Story Spine review package. They are committed as stable showcase evidence; the original run remains outside version control because production packages live under ignored output roots.

The three Personal-IP frames demonstrate one consistent character and layout system across opening, teaching, and closing states. The covers demonstrate independent native 16:9, 9:16, and 1:1 compositions rather than crops of one master image.

## Candidate visual series

The images under `visual-series/` are examples from the nine specialized visual-series leaf Skills. Their README status is deliberately **candidate**: a production run must still bind source provenance and pass text and visual QC before using an image as final material.
